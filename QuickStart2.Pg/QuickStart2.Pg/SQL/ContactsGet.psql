SELECT shd.shardId() As ContactShardId, Contacts.ContactId, Contacts.FullName 
FROM shd.Contacts
	INNER JOIN @ContactKeys As ContactKeys
	ON ContactKeys.RecordId = Contacts.ContactId
		AND ContactKeys.ShardId = shd.shardId();
